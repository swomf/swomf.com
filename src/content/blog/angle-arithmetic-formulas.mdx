---
title: "Angle arithmetic formulas"
description: "Prove sin(α+β), cos(α+β) with a picture; then, derive the rest"
tags:
  - math
pubDate: "Apr 21 2024"
updatedDate: "May 01 2024"
heroImage: "/src/assets/images/angle-arithmetic-formulas.jpg"
---

To find out $sin(\alpha+\beta)$, let's build a diagram of
what we're actually, trigonometrically doing.

<div class="grid grid-cols-2 gap-4">
  <img src="/src/assets/images/angle-arithmetic-formulas-01.svg" alt="step 1" class="mx-auto" loading="lazy"/>
  <div>
    Let's make a triangle with angle $\beta$ on it.
    
    If its hypotenuse is some number $H$, then:  
    $opposite=H \bullet sin(\beta)$  
    $adjacent=H \bullet cos(\beta)$

    Keep this in mind for later.

  </div>
  <img src="/src/assets/images/angle-arithmetic-formulas-02.svg" alt="step 2" class="mx-auto" loading="lazy"/>
  <div>
    On top of its hypotenuse, let's add a triangle with angle $\alpha$ on it.

    Using the same intuition from $\triangle\beta$,
    we could figure out its sides, given its hypotenuse...

  </div>
  <img src="/src/assets/images/angle-arithmetic-formulas-03.svg" alt="step 3" class="mx-auto" loading="lazy"/>
  <div>
    What's this? Just by stacking triangles, we've made a
    third triangle out of $\alpha + \beta$! Let's just draw
    a dotted line so that we realize it's there.

    Remember the intuition we talked about, where all we
    need is the hypotenuse to find the sides?

  </div>
  <img src="/src/assets/images/angle-arithmetic-formulas-04.svg" alt="step 4" class="mx-auto" loading="lazy"/>
  <div>
    Let's set the hypotenuse of $\triangle(\alpha+\beta)$
    to $1$. This way, if we find its sides, we'll have
    *both*:  
  
    $1 \bullet cos(\alpha+\beta)$  
    $1 \bullet sin(\alpha+\beta)$

    Immaculate.

  </div>
  <img src="/src/assets/images/angle-arithmetic-formulas-05.svg" alt="step 5" class="mx-auto" loading="lazy"/>
  <div>
    However, when we extend the logic to $\triangle\beta$,
    we notice that our goal, the sides of
    $\triangle(\alpha+\beta)$, is still out of reach.

    Now, we have to get *crafty.* Let's get the *very top angle*.

  </div>
  <img src="/src/assets/images/angle-arithmetic-formulas-06.svg" alt="step 6" class="mx-auto" loading="lazy"/>
  <div>
    If we think back to geometry, we can draw a line
    parallel to the bottom line to discover a secret 4th
    right triangle. Parallel lines crossed by a
    transversal!

    Now all we do is hop between $\beta$ and $90-\beta$
    to get to the top.

  </div>
  <img src="/src/assets/images/angle-arithmetic-formulas-07.svg" alt="step 7" class="mx-auto" loading="lazy"/>
  <div>
    With $\angle\beta$ and the hypotenuse $sin(\alpha)$,
    we solve for the sides of our 4th triangle...  
    
    And just like that, we have the final things we
    need to solve for $\triangle(\alpha+\beta)$.
  </div>
  <img src="/src/assets/images/angle-arithmetic-formulas-08.svg" alt="step 8" class="mx-auto" loading="lazy"/>
  <div>
    Let's do addition and subtraction!
    {/* Keep these as single-dollar-sign oneliners for word wrap. */}
    $cos(\alpha+\beta)=cos\alpha cos\beta - sin\alpha sin\beta$

    $sin(\alpha+\beta)=sin\alpha cos\beta + cos\alpha sin\beta$

    For the basic angle-addition formulas, $Q.E.D.$~

  </div>
</div>

## Not tired yet? Let's use what we have to derive the others!

With just the two formulas above, we can build everything else:

- angle addition formula for $tan(\theta)$
- angle subtraction formulas
- double-angle formulas
- half-angle formulas

### angle addition formula (tangent)

Just apply
$$tan(\theta) = \frac{sin(\theta)}{cos(\theta)}$$
and factor.

$$
\begin{align}
tan(\alpha + \beta) &= \frac{
  sin(\alpha + \beta)
}{
  cos(\alpha + \beta)
} \notag \\
tan(\alpha + \beta) &= \frac{
  sin\alpha cos\beta + cos\alpha sin\beta
}{
  cos\alpha cos\beta - sin\alpha sin\beta
} \notag \\
tan(\alpha + \beta) &= \frac{
  (cos\alpha cos\beta)(
    \frac{sin\alpha cos\beta}{cos\alpha cos\beta} +
    \frac{cos\alpha sin\beta}{cos\alpha cos\beta})
}{
  (cos\alpha cos\beta)(
    \frac{cos\alpha cos\beta}{cos\alpha cos\beta} -
    \frac{sin\alpha sin\beta}{cos\alpha cos\beta}
  )
} \notag \\
tan(\alpha + \beta) &= \frac{
  tan\alpha - tan\beta
}{
  1 + tan\alpha tan\beta
}
\end{align}
$$

## angle subtraction formulas

Remember what odd and even functions are?

- **Even**: reflects over the y-axis (cosine)
- **Odd**: reflects over the origin, aka 180 degree rotation (sine, tangent)

<img
  src="/src/assets/images/angle-arithmetic-formulas-09.svg"
  alt="sine and cosine evenness"
  class="mx-auto"
  loading="lazy"
/>

This leads to the following properties:

$$
\begin{align}
sin(-\theta) &= -sin(\theta) \notag \\
cos(-\theta) &= cos(\theta) \notag \\
tan(-\theta) &= -tan(\theta) \notag
\end{align}
$$

With this, the angle-subtraction formulas become simple substitution.

$$
\begin{align}
cos(\alpha + -\beta) &= cos(\alpha) cos(\beta) - sin(\alpha) sin(-\beta) \notag \\
cos(\alpha - \beta)  &= cos\alpha cos\beta - sin\alpha (-sin\beta) \notag \\
cos(\alpha - \beta)  &= cos\alpha cos\beta + sin\alpha sin\beta \\
\notag \\
sin(\alpha + -\beta) &= sin(\alpha) cos(-\beta) + cos(\alpha) sin(-\beta) \notag \\
sin(\alpha - \beta)  &= sin\alpha cos\beta - cos\alpha sin\beta
\\
\notag \\
tan(\alpha + -\beta) &= \frac{
  tan\alpha - tan(-\beta)
}{
  1 + tan\alpha tan(-\beta)
} \notag \\
tan(\alpha - \beta) &= \frac{
  tan\alpha + tan\beta
}{
  1 - tan\alpha tan\beta
}
\end{align}
$$

### double angle formulas

These are basically the addition formulas.

Note that $cos(2\alpha)$ has two alternate forms because of the
Pythagorean Identity,  
$sin^2 \theta + cos^2 \theta = 1$.

$$
\begin{align}
sin(2\alpha) &= sin(\alpha + \alpha) \notag \\
  &= sin(\alpha)cos(\alpha) + sin(\alpha)cos(\alpha) \notag \\
sin2\alpha &= 2sin\alpha cos\alpha \\
\notag \\
cos(2\alpha) &= cos(\alpha + \alpha) \notag \\
  &= cos(\alpha)cos(\alpha) - sin(\alpha)sin(\alpha) \notag \\
cos2\alpha &= cos^2 \alpha - sin^2 \alpha \\
\notag \\
  & \quad \text{Alternate } cos2\alpha \text{ forms:} \notag \\
cos2\alpha &= cos^2 \alpha - sin^2 \alpha \quad \quad \text{alt 1: sine-only} \notag \\
  &= (1 - sin^2 x) - sin^2\alpha \notag \\
cos2\alpha &= 1 - 2sin^2\alpha \\
cos2\alpha &= cos^2 \alpha - sin^2 \alpha \quad \quad \text{alt 2: cosine-only} \notag \\
  &= cos^2\alpha - (1 - cos^2\alpha) \notag \\
cos2\alpha &= 2cos^2\alpha - 1 \\
\notag \\
tan(2\alpha) &= tan(\alpha + \alpha) \notag \\
tan(2\alpha) &= \frac{
  tan\alpha + tan\alpha
}{
  1 - tan\alpha tan\alpha
} \notag \\
tan2\alpha &= \frac{2tan\alpha}{1 - tan^2\alpha}
\end{align}
$$

### half angle formulas

Because the $cos$ double-angle formula can be expressed in both
a $sin$-only form and a $cos$-only form, we can use $cos2\alpha$ to solve
for the half-angle formulas.

$$
\begin{align}
cos2\alpha &= 1 - 2sin^2(\alpha) \quad \text{ Sine-friendly version} \notag \\
cos\alpha &= 1 - 2sin^2(\frac{\alpha}{2}) \quad \text{Solve for } sin(\frac{\alpha}{2}) \notag \\
1 - cos\alpha &= 2sin^2(\frac{\alpha}{2}) \notag \\
\frac{1 - cos\alpha}{2} &= sin^2(\frac{\alpha}{2}) \notag \\
\pm \sqrt{\frac{1 - cos\alpha}{2}} &= sin(\frac{\alpha}{2}) \\
\notag \\
cos2\alpha &= 2cos^2\alpha - 1 \quad \text{ Cosine-friendly version} \notag \\
cos\alpha &= 2cos^2(\frac{\alpha}{2}) - 1 \quad \text{Solve for } cos(\frac{\alpha}{2}) \notag \\
cos\alpha + 1 &= 2cos^2(\frac{\alpha}{2}) \notag \\
\frac{cos\alpha + 1}{2} &= cos^2(\frac{\alpha}{2}) \notag \\
\pm \sqrt{\frac{1 + cos\alpha}{2}} &= cos(\frac{\alpha}{2}) \\
\end{align}
$$

_Hey, what does the plus-minus mean?_

If a half-angle formula has a $\pm$ in it, we need to consider
the quadrant of where the new angle ends up. For example, $cos(100 \degree)$
in quadrant II is negative, but $cos(100 \degree \div 2)$ in quadrant I is positive.

For tangent, we once again apply $$tan\theta = \frac{sin\theta}{cos\theta}$$:

$$
\begin{align}
tan(\frac{\alpha}{2}) &= \frac{
    sin(\frac{\alpha}{2})
  }{
    cos(\frac{\alpha}{2})
  } \notag \\
  &= \frac{
    \pm\sqrt{\frac{1 - cos\alpha}{2}}
  }{
    \pm\sqrt{\frac{1 + cos\alpha}{2}}
  } \notag \\
  &= \frac{
    \pm\sqrt{1 - cos\alpha}
  }{
    \pm\sqrt{1 + cos\alpha}
  } \text{ Rationalize the denominator!} \notag \\
  &= \frac{\pm\sqrt{1 - cos\alpha}}{\pm\sqrt{1 + cos\alpha}} \bullet
  \frac{\sqrt{1 - cos\alpha}}{\sqrt{1 - cos\alpha}} \notag \\
  &= \frac{\pm\sqrt{(1 - cos\alpha)^2}}{\pm\sqrt{1^2 - cos^2\alpha}} \notag \\
  &= \frac{\pm\sqrt{(1 - cos\alpha)^2}}{\pm\sqrt{sin^2\alpha}} \quad \text{ Pythag. Identity}\notag \\
tan(\frac{\alpha}{2}) &= \frac{1 - cos\alpha}{sin\alpha}
\end{align}
$$

The $^2$ sign within the $$\sqrt{}$$ makes the $$\sqrt{}$$ positive, removing the need for $\pm$.
